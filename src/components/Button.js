import styles from '../../styles/Button.module.css'
const Button = () => {
    return <button type="button" className={styles.prettierButton}>
        Well bro this is just the begining of our tutorial series
    </button>   
}

export default Button;